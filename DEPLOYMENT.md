# Руководство по развертыванию проекта

## 📋 Содержание

1. [Подготовка окружения](#подготовка-окружения)
2. [Настройка базы данных (MySQL)](#настройка-базы-данных-mysql)
3. [Настройка переменных окружения](#настройка-переменных-окружения)
4. [Установка и запуск backend части](#установка-и-запуск-backend-части)
5. [Установка и запуск frontend части](#установка-и-запуск-frontend-части)
6. [Процесс сборки](#процесс-сборки)
7. [Рекомендации по развертыванию на продакшене](#рекомендации-по-развертыванию-на-продакшене)
8. [Процесс обновления](#процесс-обновления)

---

## Подготовка окружения

### Требования к системе

- **Node.js**: версия 18.0 или выше (рекомендуется использовать LTS версию)
- **npm**: версия 8.0 или выше
- **MySQL**: версия 8.0 или выше
- **Git**: для клонирования репозитория

### Установка зависимостей

1. Установите Node.js с официального сайта [nodejs.org](https://nodejs.org/)
2. Убедитесь, что npm установлен:
   ```bash
   npm -v
   ```

3. Установите Git (если еще не установлен):
   ```bash
   # Для Ubuntu/Debian
   sudo apt install git
   
   # Для CentOS/RHEL
   sudo yum install git
   
   # Для Windows - скачайте с https://git-scm.com/
   ```

4. Установите MySQL (если еще не установлен):
   - Следуйте инструкции на [официальном сайте MySQL](https://dev.mysql.com/downloads/mysql/)
   - Убедитесь, что сервис MySQL запущен

---

## Настройка базы данных (MySQL)

### Создание базы данных

1. Подключитесь к MySQL:
   ```bash
   mysql -u root -p
   ```

2. Создайте базу данных:
   ```sql
   CREATE DATABASE shop CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```

3. Создайте пользователя и предоставьте ему права:
   ```sql
   CREATE USER 'shop_user'@'localhost' IDENTIFIED BY 'your_password';
   GRANT ALL PRIVILEGES ON shop.* TO 'shop_user'@'localhost';
   FLUSH PRIVILEGES;
   ```

4. Выйдите из MySQL:
   ```sql
   EXIT;
   ```

### Инициализация структуры базы данных

База данных будет автоматически инициализирована при первом запуске сервера. Приложение создаст все необходимые таблицы.

---

## Настройка переменных окружения

### Серверная часть

Создайте файл `.env` в директории `server/` на основе файла `server/.env.example`:

```bash
cd server
cp .env.example .env
```

Отредактируйте файл `.env`:

```env
NODE_ENV=production
PORT=3000
# MySQL Configuration
DB_HOST=localhost
DB_USER=shop_user
DB_PASSWORD=your_password
DB_NAME=shop
DB_PORT=3306
```

### Описание переменных:

- `NODE_ENV` - режим окружения (development/production)
- `PORT` - порт, на котором будет работать сервер
- `DB_HOST` - хост базы данных
- `DB_USER` - имя пользователя для подключения к базе данных
- `DB_PASSWORD` - пароль пользователя базы данных
- `DB_NAME` - имя базы данных
- `DB_PORT` - порт базы данных (по умолчанию 306)

---

## Установка и запуск backend части

### Установка зависимостей

```bash
cd server
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

### Запуск в production режиме

```bash
npm start
```

### Проверка работоспособности

После запуска сервера проверьте доступность API:

```bash
curl http://localhost:3000/api/admin/health
```

Ожидаемый ответ:
```json
{
  "status": "OK",
  "timestamp": "2023-01-01T00:00:00.000Z",
  "version": "1.0.0"
}
```

---

## Установка и запуск frontend части

### Установка зависимостей

```bash
cd client
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

### Сборка для production

```bash
npm run build
```

Собранные файлы будут находиться в папке `client/dist/`

---

## Процесс сборки

### Сборка всего приложения

1. Соберите frontend:
   ```bash
   cd client
   npm run build
   ```

2. Скопируйте содержимое `client/dist/` в `server/public/`:
   ```bash
   cp -r client/dist/* server/public/
   ```

3. Запустите сервер:
   ```bash
   cd server
   npm start
   ```

Теперь приложение будет обслуживать как API, так и статические файлы фронтенда.

### Docker сборка (опционально)

Для сборки приложения в Docker контейнер:

```dockerfile
FROM node:18-alpine

WORKDIR /app

# Copy server
COPY server/package*.json ./server/
COPY server/src ./server/src

# Copy client build
COPY client/dist ./server/public

WORKDIR /app/server
RUN npm ci --only=production

EXPOSE 3000
CMD ["node", "src/index.js"]
```

Сборка образа:
```bash
docker build -t bookkeeping-app .
```

Запуск контейнера:
```bash
docker run -d -p 300:3000 --env-file server/.env bookkeeping-app
```

---

## Рекомендации по развертыванию на продакшене

### Настройка reverse proxy

Рекомендуется использовать nginx в качестве reverse proxy:

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

### Безопасность

1. Используйте HTTPS с SSL-сертификатом
2. Настройте CORS только для доверенных доменов
3. Не храните секреты в коде
4. Используйте strong passwords для базы данных
5. Регулярно обновляйте зависимости

### Мониторинг и логирование

1. Настройте централизованное логирование
2. Мониторьте производительность сервера
3. Настройте алертинг на ошибки
4. Регулярно проверяйте доступность сервиса

### Резервное копирование

1. Регулярно создавайте резервные копии базы данных:
   ```bash
   mysqldump -u shop_user -p shop > backup_$(date +%Y%m%d_%H%M%S).sql
   ```

2. Храните резервные копии в безопасном месте
3. Проверяйте восстановление из резервной копии

---

## Процесс обновления

### Обновление приложения

1. Остановите текущий экземпляр приложения:
   ```bash
   # Если запущено через npm
   Ctrl+C
   
   # Если запущено как сервис
   pm2 stop bookkeeping-app
   ```

2. Обновите код:
   ```bash
   git pull origin main
   ```

3. Обновите зависимости:
   ```bash
   cd server
   npm install
   
   cd ../client
   npm install
   ```

4. Соберите новую версию:
   ```bash
   npm run build
   ```

5. Скопируйте сборку в public директорию:
   ```bash
   cp -r dist/* ../server/public/
   ```

6. Запустите приложение:
   ```bash
   cd ../server
   npm start
   ```

### Обновление базы данных

Если в новой версии есть изменения в структуре базы данных:

1. Сделайте резервную копию:
   ```bash
   mysqldump -u shop_user -p shop > backup_before_update.sql
   ```

2. Выполните скрипты миграции (если есть)

3. Проверьте работоспособность приложения

### Rollback

Если обновление прошло неудачно:

1. Остановите приложение
2. Восстановите резервную копию базы данных:
   ```bash
   mysql -u shop_user -p shop < backup_before_update.sql
   ```

3. Верните предыдущую версию кода:
   ```bash
   git checkout <commit-hash>
   ```

4. Пересоберите и запустите приложение
