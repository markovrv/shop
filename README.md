# 📊 Bookkeeping App — Приложение для учёта проводок

Полнофункциональное веб-приложение для управления бухгалтерскими проводками, счётами и остатками. Разработано на Node.js + Vue 3 + SQLite.

[Архитектура приложения](chart:41)

---

## 🎯 Функциональность

✅ **Управление счетами**
- Создание, редактирование, удаление счетов
- Типы счетов: активы, пассивы, капитал, доходы, расходы
- Установка начального баланса

✅ **Ведение проводок**
- Создание двойных записей (дебет/кредит)
- Редактирование и удаление проводок
- Фильтрация по датам и счетам
- Пагинация для больших объемов данных

✅ **Расчет остатков**
- Автоматический расчет балансов на любую дату
- Учет типа счета (активы возрастают при дебете, пассивы — при кредите)
- Историческая точность (расчет по состоянию на дату)

✅ **Администрирование**
- Команда перепровести все проводки
- Проверка здоровья системы
- Логирование операций

✅ **Интерфейс**
- Современный адаптивный дизайн (Vuetify)
- Поддержка светлой и тёмной темы
- Интуитивная навигация
- Уведомления об успехе/ошибках

---

## 🏗 Архитектура

### Технологический стек

**Backend:**
- Node.js 18+
- Express — веб-фреймворк
- SQLite3 — встроенная база данных
- Joi — валидация данных

**Frontend:**
- Vue 3 — фреймворк
- Vuetify 3 — компоненты Material Design
- Pinia — управление состоянием
- Vite — сборщик

**Инструменты:**
- Axios — HTTP клиент
- Day.js — работа с датами
- MDI Icons — иконки

---

## 📁 Структура проекта

```
bookkeeping-app/
├── server/                      # Backend
│   ├── src/
│   │   ├── db/                  # Подключение БД и инициализация
│   │   ├── routes/              # API маршруты
│   │   ├── middleware/          # Валидация, обработка ошибок
│   │   └── utils/               # Логирование
│   └── package.json
│
├── client/                      # Frontend
│   ├── src/
│   │   ├── components/          # Vue компоненты
│   │   ├── pages/               # Страницы приложения
│   │   ├── stores/              # Pinia хранилища
│   │   └── api/                 # HTTP клиент
│   └── package.json
│
├── API-DOCS.md                  # Документация REST API
├── ARCHITECTURE.md              # Архитектура и дизайн
├── SERVER-CODE.md               # Исходный код сервера
├── CLIENT-CODE.md               # Исходный код клиента
├── DEPLOYMENT.md                # Пошаговое руководство запуска
└── README.md                    # Этот файл
```

---

## 🚀 Быстрый старт

### Требования

- Node.js 18 или выше
- npm или yarn
- Браузер с поддержкой ES6+

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd bookkeeping-app

# Установить зависимости сервера
cd server
npm install

# Установить зависимости клиента (в отдельном терминале)
cd client
npm install
```

### Запуск

**Терминал 1 — Сервер:**
```bash
cd server
npm start
# Сервер запустится на http://localhost:3000
```

**Терминал 2 — Клиент:**
```bash
cd client
npm run dev
# Клиент откроется на http://localhost:5173
```

**Открыть в браузере:**
```
http://localhost:5173
```

---

## 📚 Документация

### Основные документы

| Документ | Описание |
|----------|---------|
| **API-DOCS.md** | Полная документация REST API с примерами curl |
| **ARCHITECTURE.md** | Архитектура, компоненты, поток данных |
| **SERVER-CODE.md** | Исходный код всех файлов сервера |
| **CLIENT-CODE.md** | Исходный код всех файлов клиента |
| **DEPLOYMENT.md** | Пошаговое руководство по установке и запуску |

### Быстрые ссылки

- API базовый URL: `http://localhost:3000/api`
- Документация API: см. `API-DOCS.md`
- Примеры curl: в конце `API-DOCS.md`

---

## 💻 Примеры использования

### Создать счет через API

```bash
curl -X POST http://localhost:3000/api/accounts \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Касса",
    "type": "asset",
    "initialBalance": 50000
  }'
```

### Создать проводку

```bash
curl -X POST http://localhost:3000/api/entries \
  -H "Content-Type: application/json" \
  -d '{
    "date": "2025-12-14",
    "description": "Продажа товара",
    "debitAccountId": 1,
    "creditAccountId": 2,
    "amount": 1000
  }'
```

### Получить остатки

```bash
curl http://localhost:3000/api/balances?date=2025-12-14
```

### Перепровести все проводки

```bash
curl -X POST http://localhost:3000/api/admin/recalculate
```

---

## 🔐 Валидация и безопасность

### На сервере
- ✅ Валидация всех входных данных (Joi)
- ✅ Защита от SQL injection (prepared statements)
- ✅ CORS для разработки и production
- ✅ Обработка и логирование ошибок

### На клиенте
- ✅ Валидация форм перед отправкой
- ✅ XSS защита (Vue экранирует текст)
- ✅ Правильное управление состоянием (Pinia)

---

## 📊 Логика расчета баланса

Для каждого счета баланс считается как:

```
Активы и расходы (asset, expense):
  Баланс = Начальный баланс + Дебет - Кредит

Пассивы, капитал и доходы (liability, equity, income):
  Баланс = Начальный баланс + Кредит - Дебет
```

**Пример:**
- Касса (asset, начальный баланс 10000)
- Операция: Дебет Касса 1000, Кредит Выручка
- Новый баланс Кассы: 10000 + 1000 = 11000

---

## 🛠 Troubleshooting

### Проблема: "Port 3000 is already in use"

**Решение:**
```bash
# Найти процесс на порту
lsof -i :3000

# Убить процесс
kill -9 <PID>

# Или использовать другой порт в .env
PORT=3001
```

### Проблема: "Cannot find module 'sqlite3'"

**Решение:**
```bash
cd server
rm -rf node_modules package-lock.json
npm install
```

### Проблема: "CORS error"

**Решение:**
- Убедитесь, что сервер работает на `http://localhost:3000`
- Проверьте `client/src/api/client.js` — правильный baseURL
- Проверьте CORS настройки в `server/src/index.js`

### Проблема: "Database is locked"

**Решение:**
- Перезагрузите приложение
- Выполните `POST /api/admin/recalculate`
- Проверьте, нет ли других процессов, использующих БД

---

## 📈 Производительность

- Пагинация: 50 записей на странице (настраивается)
- Индексы БД для быстрого поиска по датам и счетам
- Кэширование на клиенте (Pinia store)
- Ленивая загрузка компонентов (Vue)

---

## 🔄 Жизненный цикл проводки

1. **Создание** → Пользователь заполняет форму
2. **Валидация** → Сервер проверяет все параметры
3. **Сохранение** → Запись в БД (SQL INSERT)
4. **Обновление UI** → Клиент получает новую проводку
5. **Пересчет** → Остатки пересчитываются автоматически при запросе
6. **Редактирование** → Обновление значений (UPDATE)
7. **Удаление** → Удаление из БД (DELETE)

---

## 🚀 Production развертывание

### Сборка

```bash
# Собрать клиент для production
cd client
npm run build
# Создаст папку dist/ с оптимизированной статикой
```

### Запуск на production

```bash
# Сервер будет подавать статику из dist/
NODE_ENV=production node server/src/index.js
```

### Docker

```dockerfile
FROM node:18-alpine
WORKDIR /app

# Сборка клиента
COPY client/package*.json ./client/
WORKDIR /app/client
RUN npm ci && npm run build

# Сервер
WORKDIR /app
COPY server/package*.json ./server/
COPY server/src ./server/src
COPY --from=builder /app/client/dist ./server/public

WORKDIR /app/server
RUN npm ci --only=production

EXPOSE 3000
CMD ["node", "src/index.js"]
```

---

## 📝 Файлы БД

- **bookkeeping.db** — основная база данных (SQLite)
- **bookkeeping.db-shm** — временный файл (можно удалить)
- **bookkeeping.db-wal** — логирование (можно удалить)

Для резервной копии просто скопируйте `bookkeeping.db`.

---

## 🔄 Обновление и поддержка

### Обновить зависимости

```bash
# Сервер
cd server
npm update

# Клиент
cd client
npm update
```

### Переустановить зависимости

```bash
# Сервер
cd server
rm -rf node_modules package-lock.json
npm install

# Клиент
cd client
rm -rf node_modules package-lock.json
npm install
```

---

## 💡 Идеи для расширения

- **Аутентификация** — Multi-user с разными правами
- **Экспорт** — PDF, Excel, CSV отчеты
- **Импорт** — Загрузка проводок из файла
- **История** — Версионирование изменений
- **Аналитика** — Диаграммы и статистика
- **Синхронизация** — Облачное хранилище (Firebase, Supabase)
- **Мобильное приложение** — React Native или Flutter
- **WebSockets** — Real-time синхронизация
- **Подразделения** — Многофирменный учет
- **Налоговый отчет** — Автоматические декларации

---

## 📄 Лицензия

MIT License — см. файл LICENSE

---

## 👤 Автор

Создано как полнофункциональное приложение для управления бухгалтерскими проводками.

---

## 🤝 Вклад

Приветствуются pull requests и issues!

---

## 📞 Поддержка

При возникновении вопросов или проблем:

1. Проверьте **DEPLOYMENT.md** для инструкций по запуску
2. Проверьте **API-DOCS.md** для документации API
3. Смотрите логи в консоли браузера (F12 → Console)
4. Смотрите логи сервера (консоль Node.js)

---

## 🎓 Обучение

Это полностью учебный проект, показывающий:
- Как построить REST API на Node.js
- Как использовать SQLite для небольших приложений
- Как создать современный фронтенд на Vue 3
- Как организовать архитектуру клиент-серверного приложения
- Как использовать Pinia для управления состоянием
- Как работать с Vuetify для адаптивного дизайна

---

**Готово к использованию! Наслаждайтесь приложением для учета проводок.** 🎉

---

## Быстрые команды

```bash
# Установка и запуск за 3 минуты
cd server && npm install && npm start &
cd client && npm install && npm run dev
# Откройте http://localhost:5173

# Тестирование API
curl http://localhost:3000/api/accounts
curl http://localhost:3000/api/admin/health
curl -X POST http://localhost:3000/api/admin/recalculate
```

**Version:** 1.0.0  
**Last Updated:** 2025-12-14  
**Status:** ✅ Production Ready
